@*@using TodoListApp.ViewModels;
@model Pager



<div class="container">
	@if (Model.TotalPages > 0)
	{
		<ul class="pagination justify-content-end">
			@if (Model.Page > 1)
			{
				<li class="page-item">
					<a class="page-link" asp-action="ListTask" asp-route-pg="1">First</a>
				</li>
				<li class="page-item">
					<a class="page-link" asp-action="ListTask" asp-route-pg="@Model.Page-1">Previous</a>
				</li>
			}
			@for (var pge = Model.StartPage; pge < Model.EndPage; pge++)
			{
				<li class="page-item" @(pge == Model.Page ? "active" : "")>
					<a class="page-link" asp-action="ListTask" asp-route-pg="@pge">@pge</a>
				</li>
			}
			@if (Model.Page < Model.TotalPages)
			{
				<li class="page-item">
					<a class="page-link" asp-action="ListTask" asp-route-pg="Model.Page+1">Next</a>
				</li>
				<li class="page-item">
					<a class="page-link" asp-action="ListTask" asp-route-pg="@Model.TotalPages">Last</a>
				</li>
			}
		</ul>
	}
</div>
*@